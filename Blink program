/*
  Goal:
  Blinks the LED on pin 9 of the Arduino board, turning it on for 1 second and off for 1 second in a continuous loop.
   
  Main Theme:
  Controlling a digital output (LED) using digitalWrite and delay functions in Arduino.
*/

void setup() {
  // Initialize pin 9 as an output
  pinMode(9, OUTPUT);
}

void loop() {
  // Turn the LED on by setting the LED pin to HIGH
  digitalWrite(9, HIGH);   
  
  // Wait for 1 second 
  delay(1000);                       
  
  // Turn the LED off by setting the LED pin to LOW
  digitalWrite(9, LOW);    
  
  // Wait for 1 second
  delay(1000);                       
}
