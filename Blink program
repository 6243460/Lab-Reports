/*
  Goal:
  Blinks the LED on pin 9 of the Arduino board, turning it on for 1 second and off for 1 second in a continuous loop.
   
  Main Theme:
  Controlling a digital output (LED) using digitalWrite and delay functions in Arduino.
*/

void setup() {
  pinMode(9, OUTPUT);  // Set pin 9 as an output 
}

void loop() {
  digitalWrite(9, !digitalRead(9));  // Read the current state of the LED, if the LED is on, turn it off. If off, turn it on.
  delay(1000);  // Wait for 1 second
}
